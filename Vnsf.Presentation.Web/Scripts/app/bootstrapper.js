define('bootstrapper',
    ['jquery', 'config'],
    function ($, config) {
        var
            run = function () {
                
                
            };

        return {
            run: run
        };
    });