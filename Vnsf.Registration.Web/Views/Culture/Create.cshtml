@model Vnsf.Service.Data.Messaging.ConfigurationService.CreateCultureRequest


@{
    ViewBag.Title = "CreateCulture";
}

<h2>CreateCulture</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CultureData</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Data.Code)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Data.Code)
            @Html.ValidationMessageFor(model => model.Data.Code)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Data.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Data.Name)
            @Html.ValidationMessageFor(model => model.Data.Name)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
